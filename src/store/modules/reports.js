import reports from '../../api/reports'
import * as types from '../mutation-types'

//state
const state = {
    cities: []
}

// getters
const getters = {
   
}

// actions
const actions = {
   
}

// mutations
const mutations = {
    
}

export default {
    state,
    getters,
    actions,
    mutations
}